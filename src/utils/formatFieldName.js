export const formatFieldName = (field) => {
  if (field === "DEEPLEARNING") {
    return "DEEP LEARNING" ;
  } else if (field === "DATAANALYSIS") {
    return "DATA ANALYSIS";
  } else {
    return field;
  }
};
